import 'package:flutter/material.dart';

class CGPAScreen extends StatelessWidget {
  const CGPAScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child :Text('This is CGPA Screen'),
    );
  }
}